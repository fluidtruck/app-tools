description: >
  An executor for gcloud commands.
docker:
  - image: 'google/cloud-sdk:<<parameters.tag>>'
parameters:
  tag:
    default: latest
    description: >
      Pick a specific google/cloud-sdk image variant:
      https://hub.docker.com/r/google/cloud-sdk/tags
    type: string
